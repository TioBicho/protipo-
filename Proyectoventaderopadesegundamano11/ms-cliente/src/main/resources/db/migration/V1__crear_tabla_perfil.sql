CREATE TABLE perfil (
                        id_perfil NUMBER(8)    NOT NULL,
                        telefono  NVARCHAR2(12) NOT NULL,
                        CONSTRAINT perfil_pk PRIMARY KEY (id_perfil)
);